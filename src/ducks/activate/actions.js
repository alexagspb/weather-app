import { createActions } from "redux-actions";

export const { setWeatherActive } = createActions("SET_WEATHER_ACTIVE");
export const { removeWeatherSuccess } = createActions("REMOVE_WEATHER_SUCCESS");
